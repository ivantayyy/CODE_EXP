import React, { Component } from "react";
import {
  Text,
  View,
  StyleSheet,
  Image,
  TouchableOpacity,
  TextInput,
} from "react-native";

class Validation extends Component {
  state = {
    email: "",
    password: "",
  };
  //handleEmail = (text) => {
  //this.setState({ email: text });
  //};
  //handlePassword = (text) => {
  //this.setState({ password: text });
  //};
  //login = (email, pass) => {
  //alert("email: " + email + " password: " + pass);
  //};

  // can send this information for validating the user

  render() {
    return <Text> </Text>;
  }
}
export default Validation;
